//                             (ternary operator)

/* The ternary operator: Its a short way to write conditional sentences,
    and its called the ternary operator because it consists of three parts,
    and its structure is as follows:

var e = (condition) ? "If result is positive,this will happen" : "If result is negative, this will happen";

Example: */

var age = 18;

var voteable = (age < 18) ? "yes" : "no";

alert(voteable);